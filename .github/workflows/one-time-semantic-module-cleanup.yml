name: One-time semantic module cleanup

on:
  push:
    branches:
      - test/homologacao-clean-ui
    paths:
      - scripts/semantic-module-cleanup.mjs

permissions:
  contents: write

concurrency:
  group: semantic-module-cleanup
  cancel-in-progress: false

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: test/homologacao-clean-ui
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Rename semantic modules and references
        run: node scripts/semantic-module-cleanup.mjs

      - name: Validate JavaScript syntax
        shell: bash
        run: find js -type f -name '*.js' -print0 | xargs -0 -n1 node --check

      - name: Validate architecture
        run: node scripts/check-homologation-architecture.mjs

      - name: Validate canonical build
        env:
          APP_ENV: homologation
        run: node scripts/build-site.mjs site-dist

      - name: Remove generated build
        run: rm -rf site-dist

      - name: Commit cleanup
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A admin portal js .github/workflows/real-data-ci.yml scripts/check-homologation-architecture.mjs
          git diff --cached --quiet && exit 0
          git commit -m 'refactor: replace versioned modules with semantic names'
          git push origin HEAD:test/homologacao-clean-ui
