name: One-time login consolidation

on:
  push:
    branches:
      - test/homologacao-clean-ui
    paths:
      - scripts/consolidate-login.mjs

permissions:
  contents: write

concurrency:
  group: login-consolidation
  cancel-in-progress: false

jobs:
  consolidate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: test/homologacao-clean-ui
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Consolidate login scripts
        run: node scripts/consolidate-login.mjs

      - name: Validate JavaScript syntax
        shell: bash
        run: find js -type f -name '*.js' -print0 | xargs -0 -n1 node --check

      - name: Validate architecture
        run: node scripts/check-homologation-architecture.mjs

      - name: Validate canonical build
        env:
          APP_ENV: homologation
        run: node scripts/build-site.mjs site-dist

      - name: Remove generated build
        run: rm -rf site-dist

      - name: Commit consolidation
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html js/login.js js/login-round3.js js/login-round4.js scripts/check-homologation-architecture.mjs
          git diff --cached --quiet && exit 0
          git commit -m 'refactor: consolidate login behavior'
          git push origin HEAD:test/homologacao-clean-ui
