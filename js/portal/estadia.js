function stayFullDate(value){const iso=typeof portalIsoDate==='function'?portalIsoDate(value):String(value||'').slice(0,10);if(!iso)return '—';const locale=typeof currentLocale==='function'?currentLocale():'pt-BR';return new Intl.DateTimeFormat(locale,{day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(iso+'T12:00:00'))}
function volunteerStay(){
  const application=state.currentApplication||{};
  const unit=application.unitName||String(application.unitId||'—').replace(/^./,c=>c.toUpperCase());
  return `<section class="section"><div class="section-head"><div><span class="eyebrow">${escapeHtml(unit)}</span><h2>Minha estadia</h2><p>Informações do período</p></div></div><div class="grid-2"><div class="card"><span class="eyebrow">Chegada</span><strong style="display:block;margin-top:8px">${stayFullDate(application.stayStart)}</strong></div><div class="card"><span class="eyebrow">Saída</span><strong style="display:block;margin-top:8px">${stayFullDate(application.stayEnd)}</strong></div></div><div class="card" style="margin-top:10px"><h3 style="font-size:.8rem">Unidade ${escapeHtml(unit)}</h3><p style="font-size:.67rem;color:var(--muted);margin-top:5px">A equipe local acompanha a experiência e pode ajustar datas e atividades conforme a rotina da comunidade.</p><div class="activity-actions"><button class="btn btn-soft" type="button" onclick="navigateVolunteer('info')"><i class="fa-solid fa-route"></i>Orientações</button></div></div></section>`
}
