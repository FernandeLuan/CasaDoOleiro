/* Round 32/33 — final Admin standard: day = state; activity = context/reason/actions. */
(function reviewFlowR32Admin(){
  const R=window.OleiroR31AdminReview;if(!R)return;
  const baseAdminPlanningDayCard=window.adminPlanningDayCard||adminPlanningDayCard;
  const baseStatusMeta=R.statusMeta;
  const safe=value=>encodeURIComponent(String(value??''));
  function timeMs(value){if(!value)return 0;try{if(typeof value.toMillis==='function')return Number(value.toMillis())||0;if(typeof value.toDate==='function')return Number(value.toDate().getTime())||0}catch{}const parsed=Date.parse(String(value));return Number.isFinite(parsed)?parsed:0}
  function adjustmentReady(session){if(!session||session.adminAdjustmentStatus!=='requested')return false;if(session._r32AdjustmentReady===true)return true;const requested=timeMs(session.adminAdjustmentRequestedAt),updated=timeMs(session.updatedAt);return requested>0&&updated>requested}
  R.adjustmentReady=adjustmentReady;
  R.statusMeta=function(session){if(adjustmentReady(session))return {badges:'<span class="badge success">Ajustado</span>',classes:'r32-card-ready'};return baseStatusMeta(session)};
  function dayState(rows){const requested=rows.filter(r=>r.adminAdjustmentStatus==='requested');if(requested.some(r=>!adjustmentReady(r)))return {tone:'warning',label:'Reajustar'};if(requested.length&&requested.every(adjustmentReady))return {tone:'success',label:'Ajustado'};if(rows.some(r=>r.status==='change_requested'&&r.changeReviewStatus==='adjustments')||rows.some(r=>r.postApprovalProposal===true&&r.reviewStatus==='adjustments'))return {tone:'warning',label:'Reajustar'};if(rows.some(r=>r.status==='change_requested'&&(r.changeReviewStatus||'analysis')==='analysis'))return {tone:'info',label:'Alteração'};if(rows.some(r=>r.postApprovalProposal===true&&(r.reviewStatus||'analysis')==='analysis'))return {tone:'info',label:'Nova atividade'};if(rows.some(r=>r.adminAdjustmentStatus==='analysis'))return {tone:'info',label:'Reenviado'};if(rows.some(r=>r.changeReviewStatus==='rejected'||(r.postApprovalProposal===true&&r.reviewStatus==='rejected')))return {tone:'danger',label:'Recusada'};return null}
  R.daySignals=function(day){const meta=dayState(day?.sessions||[]);return meta?`<span class="r33-day-state ${meta.tone}">${escapeHtml(meta.label)}</span>`:''};
  function marker(session){if(session.adminAdjustmentStatus==='requested')return {tone:adjustmentReady(session)?'success':'warning',label:adjustmentReady(session)?'Atividade ajustada':'Atividade com ajuste solicitado'};if(session.adminAdjustmentStatus==='analysis')return {tone:'info',label:'Ajuste reenviado'};if(session.status==='change_requested')return {tone:session.changeReviewStatus==='adjustments'?'warning':'info',label:session.changeReviewStatus==='adjustments'?'Reajuste solicitado':'Mudança solicitada'};if(session.postApprovalProposal===true){if(session.reviewStatus==='adjustments')return {tone:'warning',label:'Reajuste solicitado'};if(session.reviewStatus==='rejected')return {tone:'danger',label:'Atividade recusada'};if((session.reviewStatus||'analysis')==='analysis')return {tone:'info',label:'Nova atividade proposta'}}return null}
  function markerHtml(meta){return meta?`<span class="r33-session-marker ${meta.tone}" role="img" aria-label="${escapeHtml(meta.label)}" title="${escapeHtml(meta.label)}"><i class="fa-solid fa-circle-info" aria-hidden="true"></i></span>`:''}
  function ensureReason(card,session){if(session.adminAdjustmentStatus!=='requested'||!session.adminAdjustmentNote||card.querySelector('.r31-review-details'))return;const main=card.querySelector('.admin-portal-activity-main'),actions=main?.querySelector('.admin-portal-actions');if(!main)return;const divider=document.createElement('div'),details=document.createElement('div');divider.className='admin-portal-detail-divider r31-review-divider';divider.setAttribute('aria-hidden','true');details.className='r31-review-details';details.innerHTML=`<div class="r31-review-block warning"><strong>Motivo do ajuste:</strong><p>${escapeHtml(session.adminAdjustmentNote)}</p></div>`;main.insertBefore(divider,actions||null);main.insertBefore(details,actions||null)}
  function ensureActions(p,session,card){const target=card.querySelector('.admin-portal-actions')||card.querySelector('.admin-portal-activity-main');if(!target||!session?.id)return;if(session.postApprovalProposal===true&&(session.reviewStatus||'analysis')==='analysis'&&!card.querySelector('.post-approval-admin-actions')&&session.activityId){const app=safe(p.id),activity=safe(session.activityId);target.insertAdjacentHTML('beforeend',`<div class="post-approval-admin-actions"><button class="btn btn-primary btn-xs" type="button" onclick="reviewPostApprovalProposal('${app}','${activity}','approve')"><i class="fa-solid fa-check"></i>Aprovar</button><button class="btn btn-soft btn-xs" type="button" onclick="requestPostApprovalReajust('${app}','${activity}')"><i class="fa-solid fa-rotate"></i>Reajustar</button><button class="btn btn-danger-soft btn-xs" type="button" onclick="reviewPostApprovalProposal('${app}','${activity}','reject')"><i class="fa-solid fa-xmark"></i>Recusar</button></div>`)}if(session.status==='change_requested'&&(session.changeReviewStatus||'analysis')==='analysis'&&!card.querySelector('.post-approval-admin-actions')){const app=safe(p.id),sid=safe(session.id);target.insertAdjacentHTML('beforeend',`<div class="post-approval-admin-actions"><button class="btn btn-primary btn-xs" type="button" onclick="reviewR31ExistingChange('${app}','${sid}','approve')"><i class="fa-solid fa-check"></i>Aprovar</button><button class="btn btn-soft btn-xs" type="button" onclick="openR31ExistingReajust('${app}','${sid}')"><i class="fa-solid fa-rotate"></i>Reajustar</button><button class="btn btn-danger-soft btn-xs" type="button" onclick="openR31ExistingReject('${app}','${sid}')"><i class="fa-solid fa-xmark"></i>Recusar</button></div>`)}}
  adminPlanningDayCard=function(p,day){const html=baseAdminPlanningDayCard(p,day),tpl=document.createElement('template');tpl.innerHTML=html;(day?.sessions||[]).forEach(session=>{const card=tpl.content.querySelector(`.admin-portal-activity-card[data-session-id="${CSS.escape(String(session.id||''))}"]`);if(!card)return;card.querySelectorAll('.r32-session-signal-wrap,.r31-day-signal-wrap').forEach(node=>node.remove());if(adjustmentReady(session)){card.classList.remove('r31-card-warning');card.classList.add('r32-card-ready')}if(session.postApprovalProposal===true&&(session.reviewStatus||'analysis')==='analysis')card.classList.add('r32-new-activity');ensureReason(card,session);ensureActions(p,session,card);const meta=marker(session);if(meta&&!card.querySelector('.r33-session-marker')){let status=card.querySelector('.admin-portal-status');if(!status){status=document.createElement('div');status.className='admin-portal-status';card.querySelector('.admin-portal-activity-head')?.appendChild(status)}status?.insertAdjacentHTML('beforeend',markerHtml(meta))}});return tpl.innerHTML};
  window.adminPlanningDayCard=adminPlanningDayCard;
})();
