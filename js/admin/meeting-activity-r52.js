/* Round 55 — ações de reunião são injetadas após a limpeza do fluxo de seleção. */
(function adminMeetingActivityR55(){
  const baseRenderPersonModal=window.renderPersonModal||renderPersonModal;
  const safe=value=>encodeURIComponent(String(value??''));
  function meetingCandidate(p){return !!p&&!p.inactive&&p.status==='meeting'}
  function eligibleDates(p){const start=String(p?.stayStart||p?.from||'').slice(0,10),end=String(p?.stayEnd||p?.to||'').slice(0,10);return typeof planningEligibleDates==='function'?planningEligibleDates(start,end):[]}
  function sessionsForDate(p,date){return (state.sessions||[]).filter(row=>String(row.applicationId||p.id)===String(p.id)&&String(row.date||'')===String(date)).sort(typeof activityScheduleCompare==='function'?activityScheduleCompare:(a,b)=>String(a.date||'').localeCompare(String(b.date||'')))}
  function findSession(applicationId,sessionId){const direct=(state.sessions||[]).find(row=>String(row.id)===String(sessionId)&&String(row.applicationId||applicationId)===String(applicationId));if(direct)return direct;for(const [key,cache] of Object.entries(state.adminPlanPageCache||{})){if(!key.startsWith(`${applicationId}|`))continue;const row=(cache?.sessions||[]).find(item=>String(item.id)===String(sessionId));if(row)return row}return null}
  function approvedSource(session){return !!session&&session.postApprovalProposal!==true&&!['rejected','change_requested'].includes(String(session.status||''))}
  function inlineActions(p,session,date){if(!meetingCandidate(p)||!approvedSource(session)||!session?.id)return '';const app=safe(p.id),sid=safe(session.id),day=safe(date);return `<div class="admin-session-manage-actions admin-meeting-creation-actions"><button class="btn btn-outline btn-xs" type="button" onclick="openAdminReplicateActivity('${app}','${sid}','${day}')"><i class="fa-solid fa-copy"></i>Replicar atividade</button><button class="btn btn-soft btn-xs" type="button" onclick="openAdminPlanningActivity('${app}','${day}')"><i class="fa-solid fa-plus"></i>Adicionar atividade</button></div>`}
  function injectMeetingActions(p,tab){modalRoot.querySelectorAll('.admin-meeting-add-activity').forEach(node=>node.remove());if(!meetingCandidate(p)||(tab!=='plan'&&modalRoot.dataset.personTab!=='plan'))return;modalRoot.querySelectorAll('details.planning-day-card[data-plan-date]').forEach(card=>{const date=card.dataset.planDate,content=card.querySelector('.planning-day-content');if(!date||!content)return;content.querySelectorAll('.admin-meeting-creation-actions,.admin-meeting-empty-action').forEach(node=>node.remove());const rows=[...card.querySelectorAll('.planning-session-row')];if(rows.length){content.querySelectorAll('.admin-create-activity-action').forEach(node=>node.remove());const sessions=sessionsForDate(p,date);rows.forEach((row,index)=>{const actions=inlineActions(p,sessions[index],date);if(actions)(row.firstElementChild||row).insertAdjacentHTML('beforeend',actions)});return}if(eligibleDates(p).includes(date)){content.insertAdjacentHTML('beforeend',`<div class="admin-create-activity-action admin-meeting-empty-action"><button class="btn btn-soft btn-block" type="button" onclick="openAdminPlanningActivity('${safe(p.id)}','${safe(date)}')"><i class="fa-solid fa-plus"></i>Adicionar atividade</button></div>`)}})}
  window.openAdminReplicateActivity=function(encodedId,encodedSessionId,encodedDate){const id=decodeURIComponent(encodedId),sessionId=decodeURIComponent(encodedSessionId),sourceDate=decodeURIComponent(encodedDate),p=candidateById(id),session=findSession(id,sessionId);if(!meetingCandidate(p)||!approvedSource(session))return showToast('Esta atividade não pode ser replicada agora.');const dates=eligibleDates(p);if(!dates.length)return showToast('Não há dias disponíveis para replicar a atividade.');const options=dates.map(date=>`<option value="${escapeHtml(date)}" ${date===sourceDate?'selected':''}>${escapeHtml(dayName(date))} • ${escapeHtml(fmtDate(date,true))}</option>`).join('');const name=session.activityName||session.activity?.name||'Atividade';openModal('Replicar atividade',`Uma cópia de ${escapeHtml(name)} será criada como atividade independente.`,`<div class="field"><label for="adminReplicateActivityDate">Novo dia</label><select id="adminReplicateActivityDate" class="select">${options}</select></div>`,`<button class="btn btn-primary btn-block" type="button" onclick="continueAdminReplicateActivity('${safe(id)}','${safe(sessionId)}')"><i class="fa-solid fa-copy"></i>Continuar</button>`)};
  window.continueAdminReplicateActivity=function(encodedId,encodedSessionId){const id=decodeURIComponent(encodedId),sessionId=decodeURIComponent(encodedSessionId),p=candidateById(id),session=findSession(id,sessionId),date=document.getElementById('adminReplicateActivityDate')?.value||'';if(!meetingCandidate(p)||!approvedSource(session))return showToast('Esta atividade não pode ser replicada agora.');if(!eligibleDates(p).includes(date))return showToast('Escolha um dia disponível.');window.openAdminPlanningActivity(safe(id),safe(date));const activity=session.activity||{};const values={managerActName:session.activityName||activity.name||'',managerActDesc:session.activityDescription||activity.description||'',managerActDuration:Number(session.duration||activity.duration)||60,managerActGroup:session.groupId||'Livre',managerActPeriod:typeof activityPeriodValue==='function'?activityPeriodValue(session,activity):(session.period||activity.period||'Sem preferência'),managerActMaterials:session.materials||activity.materials||'',managerActNotes:session.notes||activity.notes||''};Object.entries(values).forEach(([field,value])=>{const input=document.getElementById(field);if(input)input.value=String(value)})};
  renderPersonModal=function(p,tab='overview'){const result=baseRenderPersonModal(p,tab);injectMeetingActions(p,tab);return result};window.renderPersonModal=renderPersonModal;
})();

/* R61 final: product-current.js é carregado antes deste arquivo e restaura componentes antigos.
   Reaplicamos somente a composição visual; regras e ações permanecem as mesmas. */
managerNav=function(){
  const item=(id,icon,label,action,active=false)=>`<button class="r61-nav-btn ${active?'active':''}" type="button" onclick="${action}"><i class="fa-solid ${icon}"></i><span>${label}</span></button>`;
  return `<nav class="r61-primary-nav"><div class="r61-primary-nav-inner">${item('home','fa-house','Visão geral',"navigateManager('home')",state.managerPage==='home')}${item('volunteer','fa-users','Voluntariado',"navigateManager('volunteer')",state.managerPage==='volunteer')}${item('agenda','fa-calendar-days','Agenda',"navigateManager('agenda')",state.managerPage==='agenda')}${item('occupancy','fa-bed','Ocupação','openOccupancyCalendar()')}${item('groups','fa-people-group','Grupos',"navigateManager('groups')",state.managerPage==='groups')}${item('menu','fa-ellipsis','Mais',"navigateManager('menu')",state.managerPage==='menu')}<div class="r61-nav-context"><span></span>Ambiente de gestão</div></div></nav>`;
};
managerMenu=function(){return `<div class="r61-page"><div class="r61-page-heading"><div><span class="r61-kicker">Configurações e referências</span><h1>Mais</h1><p>Ferramentas de operação, conteúdo do portal e dados da sua sessão.</p></div></div><div class="r61-menu-grid"><section class="r61-menu-section"><h3>Operação da Casa</h3><p>Itens usados no dia a dia da equipe.</p>${r61MenuItem('fa-people-group','Grupos','Integrantes e capacidade por unidade',"navigateManager('groups')")}${r61MenuItem('fa-bed','Ocupação','Voluntários hospedados por período','openOccupancyCalendar()')}${r61MenuItem('fa-clock','Rotina','Horários de referência da comunidade','openRoutine()')}</section><section class="r61-menu-section"><h3>Portal e conta</h3><p>Estrutura, conteúdo e sessão administrativa.</p>${r61MenuItem('fa-circle-info','Informações do portal','Conteúdo consultado por candidatos e voluntários','openPortalInfo()')}${r61MenuItem('fa-building','Unidades','Situação e disponibilidade das unidades','openUnits()')}${r61MenuItem('fa-user-shield','Minha conta','Dados da sessão autenticada','openMyAccount()')}${r61MenuItem('fa-right-from-bracket','Sair','Encerrar sessão neste dispositivo','logout()')}</section></div></div>`};
window.managerNav=managerNav;window.managerMenu=managerMenu;
if(state.role==='manager'&&typeof render==='function')render();
